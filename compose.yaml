services:
  dc:
    image: kasmweb/discord:latest
    pull_policy: daily
    tty: true
    stdin_open: true
    environment: 
     APP_ARGS: "firejail --noprofile --net=none"
    ports: "6901:6901"